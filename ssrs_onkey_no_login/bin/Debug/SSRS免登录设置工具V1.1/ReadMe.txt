目前测试的是完全支持SQL Server2008和SQL Server2016版本的ReportServices免登录破解；
破解步骤如下：
1、找到ReportServer的文件路径，并填写到config.ini文件中（默认已填写了SQL Server2016的默认安装路径，如果是2016版本的默认安装位置，可跳过本步骤）。
2、运行ssrs_onkey_no_login.exe程序，按照操作说明操作即可（按两次空格）。